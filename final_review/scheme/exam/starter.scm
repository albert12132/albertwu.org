; Q2
(define (count-stairways n)
  ; YOUR CODE HERE
  nil)

; Q3
(define (count-serpinski n)
  ; YOUR CODE HERE
  nil)

; Q4
(define (construct value lists)
  (if __
      __
      (cons __
            __)))

; Q5
(define (list-change total denoms)
  (cond ((= total 0) __)
        ((or (< total 0) (null? denoms)) __)
        (else (append (construct (car denoms)
                                 (list-change __
                                              __))
                      (list-change __
                                   __)))))
